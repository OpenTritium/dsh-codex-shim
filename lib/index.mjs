//#region src/index.ts
/** Host entry for the bundle's browser client contribution. */
/** Cordis plugin name. */
const name = "opentritium-codex-ui";
/** The browser loader discovers the client contribution from package metadata. */
function apply() {}
//#endregion
export { apply, name };
